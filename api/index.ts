// Vercel serverless function entry point
// Import the Express app from server directory
import app from '../server/index';

// Vercel will use this as the serverless function handler
export default app;

